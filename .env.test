# Test Environment Configuration
# This file is used during test runs to provide consistent test environment

# Email IMAP Configuration (dummy values for tests)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=test@example.com
IMAP_PASSWORD=test-password

# LLM Provider (dummy values for tests)
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-test-key
ANTHROPIC_API_KEY=sk-ant-test-key

# Model Configuration
LLM_MODEL=gpt-4-turbo-preview
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4000

# Processing Options
MAX_ARTICLES_PER_NEWSLETTER=15
SCRAPING_TIMEOUT_MS=30000
ENABLE_CACHING=true
# Output directory for generated summaries (default for tests)
OUTPUT_PATH=./output
# Options: PL, EN
OUTPUT_LANGUAGE=EN
# Options: thePrimeagen, Fireship, TheoT3, Kent C. Dodds, Dan Abramov, Scott Hanselman, Casey Muratori, Jon Blow
NARRATOR_PERSONA=thePrimeagen

# Processing options
MARK_AS_READ=true
AUTO_DELETE_AFTER_PROCESSING=false
